---
interface Props {
    text?: string;
}

const { text } = Astro.props;
---

<div
    data-testid="empty-state"
    class="text-center py-12 bg-white/70 dark:bg-slate-800/50 backdrop-blur-sm rounded-xl border border-slate-200 dark:border-slate-700"
>
    {text ? (
        <p data-testid="empty-state-text" class="text-slate-600 dark:text-slate-300">{text}</p>
    ) : (
        <slot />
    )}
</div>
